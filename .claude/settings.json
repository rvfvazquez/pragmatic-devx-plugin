{
  "marketplace": {
    "enabled": true,
    "plugin": "pragmatic-devx-plugin"
  },
  "skills": {
    "spec.create": {
      "enabled": true,
      "description": "Creates a structured technical specification document"
    },
    "spec.update": {
      "enabled": true,
      "description": "Updates an existing specification"
    },
    "spec.validate": {
      "enabled": true,
      "description": "Validates a specification for completeness and consistency"
    },
    "arch.ts.create": {
      "enabled": true,
      "description": "Creates a software architecture tech spec document for a system, module, layer, or integration"
    },
    "arch.ts.validate": {
      "enabled": true,
      "description": "Validates an architecture tech spec for completeness, consistency, and architectural soundness"
    }
  }
}
